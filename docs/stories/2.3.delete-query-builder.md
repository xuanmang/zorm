# Story 2.3: Type-Safe DELETE Query Builder

**Epic**: 2 - Complete CRUD & Transaction Support
**Story**: 2.3
**Status**: Approved

---

## Story

**As a** Zig developer,
**I want** 使用类型安全的 DELETE 构建器删除数据库记录,
**so that** 我能够安全地删除数据，并通过 WHERE 条件防止误删。

---

## Acceptance Criteria

1. **AC2.3.1**: 提供 `db.newDelete(T)` API 创建 DELETE 查询构建器
2. **AC2.3.2**: 强制要求 WHERE 条件（防止意外删除所有行），无 WHERE 时编译错误或返回错误
3. **AC2.3.3**: 支持 `.where(condition, args)` 方法指定删除条件
4. **AC2.3.4**: 提供 `.exec()` 方法执行删除，返回 `DeleteResult`（包含 rows_affected）
5. **AC2.3.5**: 支持 `.setReturning(cols)` 方法启用 RETURNING 子句，返回被删除的数据
6. **AC2.3.6**: 提供 `.execReturning(dest)` 方法执行删除并获取被删除的数据
7. **AC2.3.7**: 支持 `.whereIn()` 批量删除
8. **AC2.3.8**: 完整使用示例验证工作流程

---

## Tasks / Subtasks

- [ ] **Task 1**: 创建 DeleteQuery 结构体 (AC: 2.3.1)
  - [ ] 在 `src/query/query.zig` 中定义 `DeleteQuery(comptime T: type)` 泛型函数
  - [ ] 定义内部 Self 结构体，包含字段：
    - `allocator: Allocator`
    - `db: *DB`
    - `table_name: []const u8` (从 T 推断)
    - `where_clauses: std.ArrayList(WhereClause)`
    - `returning_columns: ?[]const []const u8`
    - `has_where: bool` (跟踪是否设置了 WHERE 条件)
  - [ ] 实现 `init()` 初始化函数

- [ ] **Task 2**: 实现 db.newDelete() 工厂方法 (AC: 2.3.1)
  - [ ] 在 `src/core/db.zig` 中添加 `newDelete(comptime T: type)` 方法
  - [ ] 使用 `getTableName(T)` 获取表名
  - [ ] 创建并返回 DeleteQuery 实例
  - [ ] 初始化 has_where 为 false
  - [ ] 添加文档注释说明用法和安全注意事项

- [ ] **Task 3**: 实现 .where() 方法 (AC: 2.3.3)
  - [ ] 添加 `where(self: *Self, condition: []const u8, args: anytype)` 方法
  - [ ] 复用 WHERE 子句处理逻辑（如 UpdateQuery.where）
  - [ ] 设置 `has_where = true` 标记
  - [ ] 支持多次调用 where()，默认使用 AND 连接
  - [ ] 添加 `whereOr()` 方法支持 OR 条件
  - [ ] 返回 `*Self` 支持链式调用

- [ ] **Task 4**: 实现 .whereIn() 方法 (AC: 2.3.7)
  - [ ] 添加 `whereIn(self: *Self, column: []const u8, values: anytype)` 方法
  - [ ] 复用 UpdateQuery.whereIn 实现逻辑
  - [ ] 生成 `WHERE {column} IN ($1, $2, ...)` 子句
  - [ ] 设置 `has_where = true` 标记
  - [ ] 支持批量删除场景

- [ ] **Task 5**: 实现强制 WHERE 检查 (AC: 2.3.2)
  - [ ] 在 `exec()` 和 `execReturning()` 方法中检查 `has_where`
  - [ ] 如果 `has_where == false`，返回 `error.MissingWhereClause`
  - [ ] 提供清晰的错误消息："DELETE without WHERE is dangerous. Use db.newRaw() if intentional."
  - [ ] 添加文档注释说明如何执行无条件删除（使用 Raw SQL）
  - [ ] **可选**: 提供 `unsafeExecAll()` 方法显式允许无 WHERE 删除

- [ ] **Task 6**: 实现 SQL 构建逻辑 (AC: 2.3.1-2.3.3)
  - [ ] 实现 `build(self: *Self)` 方法生成 DELETE SQL
  - [ ] 生成 `DELETE FROM {table_name}` 部分
  - [ ] 生成 `WHERE` 子句（合并所有 where_clauses）
  - [ ] 处理占位符编号（PostgreSQL: $1, $2, ...）
  - [ ] 收集所有参数到单一数组
  - [ ] 返回完整 SQL 字符串

- [ ] **Task 7**: 实现 .exec() 方法 (AC: 2.3.4)
  - [ ] 定义 `DeleteResult` 结构体（rows_affected: usize）
  - [ ] 实现 `exec(self: *Self)` 方法
  - [ ] 检查 `has_where`，未设置返回错误
  - [ ] 调用 `build()` 生成 SQL
  - [ ] 通过 `db.conn.execute()` 执行查询
  - [ ] 解析返回的 rows_affected
  - [ ] 返回 DeleteResult

- [ ] **Task 8**: 实现 RETURNING 支持 (AC: 2.3.5, 2.3.6)
  - [ ] 添加 `setReturning(self: *Self, cols: []const []const u8)` 方法
  - [ ] 保存 returning_columns 到结构体字段
  - [ ] 修改 `build()` 方法，添加 RETURNING 子句（如果设置）
  - [ ] 实现 `execReturning(self: *Self, dest: *std.ArrayList(T))` 方法
  - [ ] 检查 `has_where`，未设置返回错误
  - [ ] 执行查询并将结果扫描到 ArrayList
  - [ ] 用于审计日志场景（记录被删除的数据）

- [ ] **Task 9**: 实现资源清理 (AC: 2.3.1)
  - [ ] 实现 `deinit(self: *Self)` 方法
  - [ ] 释放 `where_clauses` ArrayList
  - [ ] 释放所有动态分配的内存

- [ ] **Task 10**: 编写单元测试
  - [ ] 创建 `tests/unit/delete_query_test.zig`
  - [ ] 测试基本 DELETE 语句生成
  - [ ] 测试 WHERE 条件
  - [ ] 测试 whereIn() 批量删除
  - [ ] **测试强制 WHERE 检查**（无 WHERE 应返回错误）
  - [ ] 测试 RETURNING 子句生成
  - [ ] 测试链式调用语法
  - [ ] 测试内存泄漏（testing.allocator）

- [ ] **Task 11**: 编写集成测试
  - [ ] 创建 `tests/integration/delete_test.zig`
  - [ ] 测试真实 PostgreSQL DELETE 执行
  - [ ] 测试 RETURNING 返回被删除的数据
  - [ ] 测试批量删除（WHERE IN）
  - [ ] 验证 rows_affected 准确性
  - [ ] 测试事务中的 DELETE
  - [ ] 测试无 WHERE 删除返回错误

- [ ] **Task 12**: 创建示例程序 (AC: 2.3.8)
  - [ ] 创建 `examples/delete_basic.zig`
  - [ ] 演示基本 DELETE 用法
  - [ ] 演示 RETURNING 用法（审计日志场景）
  - [ ] 演示批量删除（WHERE IN）
  - [ ] 演示 WHERE 条件组合
  - [ ] **警告注释**: 说明强制 WHERE 的安全性
  - [ ] 确保示例可编译运行

---

## Dev Notes

### 架构参考

**DeleteQuery 设计** [Source: architecture/architecture-core.md#Query Builders]:
- 遵循与 SelectQuery/UpdateQuery 相同的 Builder Pattern
- 支持 Fluent Interface（链式调用返回 *Self）
- **安全优先**: 强制要求 WHERE 条件防止误删所有行

**安全设计** [Source: architecture/architecture-core.md#Security Design]:
- DELETE without WHERE 是危险操作
- 必须显式提供 WHERE 条件
- 如需无条件删除，使用 `db.newRaw("DELETE FROM table")`

**PostgreSQL RETURNING** [Source: prd.md#FR5]:
- 删除前返回被删除的数据
- 用于审计日志或确认删除内容
- 示例：`DELETE FROM users WHERE id = $1 RETURNING *`

### 强制 WHERE 检查实现

**实现策略**:
```zig
pub fn exec(self: *Self) !DeleteResult {
    if (!self.has_where) {
        return error.MissingWhereClause;
    }
    const sql = try self.build();
    // ... 执行查询
}
```

**错误消息**:
```
error.MissingWhereClause: DELETE requires WHERE clause for safety.
To delete all rows, use: db.newRaw("DELETE FROM {table}")
```

**可选的 Escape Hatch** (如果用户强烈需要):
```zig
pub fn unsafeExecAll(self: *Self) !DeleteResult {
    // 允许无 WHERE 删除，但方法名明确警告危险
    const sql = try self.build();
    // ... 执行查询
}
```

### Bun ORM 对齐

**API 一致性** [Source: prd.md#Background Context]:
```zig
// ZORM (Zig)
const result = try db.newDelete(User)
    .where("id = ?", .{123})
    .exec();

// ZORM (Zig) - 审计日志场景
var deleted_users: std.ArrayList(User) = .{};
defer deleted_users.deinit(allocator);

try db.newDelete(User)
    .where("status = ?", .{"inactive"})
    .setReturning("*")
    .execReturning(&deleted_users);

// 记录被删除的用户
for (deleted_users.items) |user| {
    std.debug.print("Deleted user: {s}\n", .{user.email});
}

// Bun ORM (golang)
var result = await db.delete(User)
    .where({ id: 123 })
    .exec();

var deletedUsers = db.delete(User)
    .where({ status: "inactive" })
    .returning("*")
    .exec();
```

### 项目结构

**新增文件**:
- `src/query/query.zig` - 添加 DeleteQuery 定义
- `src/core/types.zig` - 添加 DeleteResult 结构体
- `tests/unit/delete_query_test.zig` - 单元测试
- `tests/integration/delete_test.zig` - 集成测试
- `examples/delete_basic.zig` - 示例程序

**修改文件**:
- `src/core/db.zig` - 添加 newDelete() 方法
- `src/zorm.zig` - 导出 DeleteQuery 和 DeleteResult

### Testing

**测试标准** [Source: architecture/coding-standards.md#测试规范]:
- **测试文件位置**: `tests/unit/delete_query_test.zig`
- **测试命名**: `test "DeleteQuery: {描述性名称}"`
- **内存泄漏检测**: 使用 `std.testing.allocator`
- **覆盖率目标**: ≥ 80%

**关键测试场景**:
1. **安全测试**: 无 WHERE 条件应返回 error.MissingWhereClause
2. **功能测试**: 有 WHERE 条件正常执行
3. **批量删除**: WHERE IN 删除多行
4. **审计日志**: RETURNING 返回被删除数据
5. **事务中删除**: 支持事务内执行

**集成测试场景**:
- 创建测试数据 → DELETE → 验证数据已删除
- DELETE with RETURNING → 验证返回的数据正确
- 测试 rows_affected 准确性
- 测试事务中 DELETE + ROLLBACK

### 错误处理

**自定义错误类型** [Source: architecture/coding-standards.md#错误处理]:
```zig
pub const ZormError = error{
    MissingWhereClause,  // DELETE without WHERE
    NoRows,              // 删除无匹配行
    // ... 其他错误
};
```

**错误场景**:
- 无 WHERE 条件 → error.MissingWhereClause
- WHERE 条件不匹配任何行 → 正常返回（rows_affected = 0）
- 数据库连接错误 → error.ConnectionClosed
- SQL 语法错误 → error.SQLSyntaxError

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-19 | v1.0 | 初始故事创建 | Bob (Scrum Master) |

---

## Dev Agent Record

_(此部分由开发代理在实现时填充)_

### Agent Model Used

_(待填充)_

### Debug Log References

_(待填充)_

### Completion Notes

_(待填充)_

### File List

_(待填充)_

---

## QA Results

_(此部分由 QA 代理填充)_
