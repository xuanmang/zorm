# Story 3.5: DROP INDEX Query Builder

**Epic**: 3 - Schema Management & Type System
**Story**: 3.5
**Status**: Draft

---

## Story

**As a** Zig developer,
**I want** 使用 DROP INDEX 构建器删除数据库索引，
**so that** 我能够在索引不再需要时清理数据库。

---

## Acceptance Criteria

1. **AC3.5.1**: 提供 `db.newDropIndex(T)` API 创建 DROP INDEX 查询构建器
2. **AC3.5.2**: 提供 `.index(name)` 方法指定要删除的索引名称
3. **AC3.5.3**: 提供 `.ifExists()` 方法添加 IF EXISTS 子句
4. **AC3.5.4**: 提供 `.cascade()` 方法添加 CASCADE 选项
5. **AC3.5.5**: 提供 `.exec()` 方法执行 DDL 语句
6. **AC3.5.6**: 完整的使用示例验证

---

## Tasks / Subtasks

- [ ] **Task 1**: 创建 DropIndexQuery 泛型结构体 (AC: 3.5.1)
  - [ ] 在 `src/schema/schema.zig` 中定义 `DropIndexQuery(comptime T: type)` 函数返回结构体类型
  - [ ] 包含字段：
    - `allocator: Allocator`
    - `db: *DB`
    - `index_name: ?[]const u8` (默认 null，必须通过 `.index()` 设置)
    - `if_exists_flag: bool` (默认 false)
    - `cascade_flag: bool` (默认 false)
  - [ ] 实现内部 `Self = @This()` 模式
  - [ ] 注意：PostgreSQL DROP INDEX 不需要表名（索引名在数据库中全局唯一）

- [ ] **Task 2**: 实现 db.newDropIndex() 工厂方法 (AC: 3.5.1)
  - [ ] 在 `src/core/db.zig` 中添加 `newDropIndex(comptime T: type)` 方法
  - [ ] 创建并返回 DropIndexQuery(T) 实例
  - [ ] 绑定到 DB 实例
  - [ ] 注意：虽然接受类型参数 T，但 DROP INDEX 只需索引名，不需要表名

- [ ] **Task 3**: 实现 .index() 链式方法 (AC: 3.5.2)
  - [ ] 添加 `index(self: *Self, name: []const u8)` 方法
  - [ ] 设置 `self.index_name = name`
  - [ ] 返回 `*Self` 支持链式调用

- [ ] **Task 4**: 实现 .ifExists() 链式方法 (AC: 3.5.3)
  - [ ] 添加 `ifExists(self: *Self)` 方法
  - [ ] 设置 `self.if_exists_flag = true`
  - [ ] 返回 `*Self` 支持链式调用

- [ ] **Task 5**: 实现 .cascade() 链式方法 (AC: 3.5.4)
  - [ ] 添加 `cascade(self: *Self)` 方法
  - [ ] 设置 `self.cascade_flag = true`
  - [ ] 返回 `*Self` 支持链式调用
  - [ ] 注意：CASCADE 级联删除依赖于该索引的对象（较少使用）

- [ ] **Task 6**: 实现 .build() SQL 生成方法
  - [ ] 添加 `build(self: *Self)` 方法
  - [ ] 验证必需参数：`index_name` 不为 null
  - [ ] 生成 SQL：`DROP INDEX [IF EXISTS] {index_name} [CASCADE]`
  - [ ] 使用 ArrayList 构建 SQL 字符串
  - [ ] 返回分配的 SQL 字符串（调用者负责释放）

- [ ] **Task 7**: 实现 .exec() 执行方法 (AC: 3.5.5)
  - [ ] 添加 `exec(self: *Self)` 方法
  - [ ] 调用 `build()` 生成 SQL
  - [ ] 使用 `self.db.conn.execute(sql)` 执行 DDL
  - [ ] 处理错误并返回结果
  - [ ] 自动释放 SQL 字符串内存

- [ ] **Task 8**: 实现资源清理
  - [ ] 实现 `deinit(self: *Self)` 方法
  - [ ] 释放所有动态分配的内存
  - [ ] 确保无内存泄漏

- [ ] **Task 9**: 编写单元测试
  - [ ] 创建 `tests/unit/drop_index_test.zig`
  - [ ] 测试 SQL 构建（基础 DROP INDEX）
  - [ ] 测试 IF EXISTS 子句
  - [ ] 测试 CASCADE 选项
  - [ ] 测试参数验证（index_name 必需）
  - [ ] 测试内存泄漏（使用 std.testing.allocator）

- [ ] **Task 10**: 编写集成测试
  - [ ] 创建 `tests/integration/drop_index_test.zig`
  - [ ] 测试删除存在的索引
  - [ ] 测试 IF EXISTS（删除不存在的索引不报错）
  - [ ] 测试 CASCADE（删除有依赖的索引）
  - [ ] 测试 AC3.5.6 中的使用示例

- [ ] **Task 11**: 创建使用示例
  - [ ] 创建 `examples/drop_index.zig`
  - [ ] 实现 AC3.5.6 中的完整示例代码
  - [ ] 确保示例可编译并运行

---

## Dev Notes

### Relevant Architecture Context

**Source Tree** [Source: docs/architecture/source-tree.md]:
```
src/
├── schema/
│   └── schema.zig         # DropIndexQuery 实现
├── core/
│   └── db.zig             # DB 工厂方法 newDropIndex()
```

**Schema Manager Design** [Source: docs/architecture/architecture-core.md#5-schema-manager]:
```zig
pub fn DropIndexQuery(comptime T: type) type {
    return struct {
        const Self = @This();
        allocator: Allocator,
        db: *DB,
        index_name: ?[]const u8,
        if_exists_flag: bool,
        cascade_flag: bool,

        pub fn index(self: *Self, name: []const u8) *Self;
        pub fn ifExists(self: *Self) *Self;
        pub fn cascade(self: *Self) *Self;
        pub fn build(self: *Self) ![]const u8;
        pub fn exec(self: *Self) !void;
        pub fn deinit(self: *Self) void;
    };
}
```

**Expected SQL Output**:
```sql
-- 基础
DROP INDEX idx_users_email

-- IF EXISTS
DROP INDEX IF EXISTS idx_users_email

-- CASCADE
DROP INDEX IF EXISTS idx_users_email CASCADE
```

### Previous Story Insights

**From Story 3.3 & 3.4**:
- DropTableQuery 已实现类似的链式 API 模式
- CreateIndexQuery 已实现索引相关的构建器
- 本 Story 可参考这些 Query 的结构和实现模式

### PostgreSQL DROP INDEX 特性

**IF EXISTS** [PostgreSQL Documentation]:
- 如果索引不存在，不报错，静默成功
- 用于幂等性脚本（可重复执行）

**CASCADE**:
- 级联删除依赖于该索引的对象
- PostgreSQL 中很少有对象依赖索引，此选项较少使用
- 与 DROP TABLE CASCADE 不同（DROP TABLE CASCADE 更常用）

**索引名全局性**:
- PostgreSQL 中索引名在数据库（schema）中全局唯一
- DROP INDEX 不需要指定表名
- 格式：`DROP INDEX index_name` （不是 `DROP INDEX table_name.index_name`）

**与 DROP TABLE 的区别**:
- DROP TABLE 可能有很多依赖（视图、外键等），CASCADE 常用
- DROP INDEX 依赖较少，CASCADE 较少使用

### Coding Standards

**链式 API 模式** [Source: docs/architecture/coding-standards.md]:
- 所有链式方法返回 `*Self`
- 支持流畅接口: `.index("name").ifExists().exec()`

**内存管理** [Source: docs/architecture/coding-standards.md#内存管理规范]:
- 所有 API 必须接受 `Allocator` 参数
- 提供 `deinit()` 方法释放资源
- 测试使用 `std.testing.allocator` 检测泄漏

### Testing Requirements

**测试文件位置** [Source: docs/architecture/coding-standards.md#测试规范]:
```
tests/
├── unit/
│   └── drop_index_test.zig       # 单元测试
└── integration/
    └── drop_index_test.zig       # 集成测试
```

**测试重点**:
- SQL 生成正确性
- IF EXISTS 子句功能
- CASCADE 选项
- 参数验证（index_name 必需）

### Key Implementation Notes

**SQL 构建示例**:
```zig
pub fn build(self: *Self) ![]const u8 {
    // 参数验证
    if (self.index_name == null) return error.IndexNameRequired;

    var sql_buf: std.ArrayList(u8) = .{};
    errdefer sql_buf.deinit(self.allocator);

    try sql_buf.appendSlice(self.allocator, "DROP INDEX ");

    if (self.if_exists_flag) {
        try sql_buf.appendSlice(self.allocator, "IF EXISTS ");
    }

    try sql_buf.appendSlice(self.allocator, self.index_name.?);

    if (self.cascade_flag) {
        try sql_buf.appendSlice(self.allocator, " CASCADE");
    }

    defer sql_buf.deinit(self.allocator);
    return self.allocator.dupe(u8, sql_buf.items);
}
```

**链式调用示例**:
```zig
var drop_idx = try db.newDropIndex(User);
defer drop_idx.deinit();

try drop_idx
    .index("idx_users_email")
    .ifExists()
    .exec();

// 生成的 SQL:
// DROP INDEX IF EXISTS idx_users_email
```

**参数验证**:
```zig
pub fn exec(self: *Self) !void {
    if (self.index_name == null) {
        return error.IndexNameRequired;
    }

    const sql = try self.build();
    defer self.allocator.free(sql);

    try self.db.conn.execute(sql);
}
```

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-19 | v1.0 | Initial story creation | Bob (Scrum Master) |

---

## Dev Agent Record

*(This section will be populated during implementation)*

---

## QA Results

*(This section will be populated during QA review)*
